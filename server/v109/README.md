# v1.0.9 payload

Agent Python source is **not** stored here.

The public installer downloads `ngx-agent-v1.0.9.zip` from the private
`NexGenX/ngx-windows-agent` GitHub Release `v1.0.9` (or from `-BundleUrl`).
