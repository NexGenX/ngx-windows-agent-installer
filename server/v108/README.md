# v1.0.8 payload

Agent Python source is **not** stored here.

The public installer downloads `ngx-agent-v1.0.8.zip` from the private
`NexGenX/ngx-windows-agent` GitHub Release (or from `-BundleUrl`).
